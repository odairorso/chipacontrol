import Login from './Login';
import Register from './Register';

function Auth() {
  // You can add logic here to switch between Login and Register
  // For now, let's just render Login
  return <Login />;
}

export default Auth;